import "package:flutter/material.dart";

class AlphaDashboardScreen extends StatelessWidget {
  Widget build(context){
    return Scaffold(
      appBar: AppBar(title: const Text("Dashboard Screen")),
    );
  }
}